# Astro relies on the esbuild and sharp install scripts to place their platform
# binaries. npm 11 blocks lifecycle scripts by default, which leaves the build
# without a usable esbuild binary on a clean clone.
allow-scripts=true
